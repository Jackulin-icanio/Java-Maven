name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Build with Maven
      run: mvn clean install

    - name: Run tests
      run: mvn test

    - name: Build Docker image
      run: |
        docker build -t my-app:${{ github.sha }} .
      env:
        DOCKER_BUILDKIT: 1

    - name: Set start time
      id: start_time
      run: echo "start_time=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_ENV

    - name: Set end time
      id: end_time
      run: echo "end_time=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_ENV

    - name: Send notification to Google Chat
      env: ${{ secrets.GOOGLE_CHAT_WEBHOOK }}
      if: always()
      run: |
        curl -X POST -H 'Content-Type: application/json' \
          -d '{
                "text": "Deployment Notification\n\n
                        **Job:** ${{ job.name }}\n
                        **Repo:** ${{ github.repository }}\n
                        **Branch:** ${{ github.ref }}\n
                        **Committed By:** ${{ github.actor }}\n
                        **Commit Message:** ${{ github.event.head_commit.message }}\n
                        **Deployment Start Time:** ${{ env.start_time }}\n
                        **Deployment End Time:** ${{ env.end_time }}"
              }' \
          $GOOGLE_CHAT_WEBHOOK_URL
